const translations=document.querySelectorAll('[data-ca][data-es]');const languageButtons=document.querySelectorAll('.lang');const menuButton=document.querySelector('.menu-toggle');languageButtons.forEach(button=>{button.addEventListener('click',()=>{const language=button.dataset.lang;document.documentElement.lang=language;translations.forEach(element=>{element.textContent=element.dataset[language]});languageButtons.forEach(item=>item.classList.toggle('active',item===button))})});menuButton.addEventListener('click',()=>{const open=document.body.classList.toggle('menu-open');menuButton.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.nav-links a').forEach(link=>{link.addEventListener('click',()=>{document.body.classList.remove('menu-open');menuButton.setAttribute('aria-expanded','false')})});
